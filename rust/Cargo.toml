[package]
name = "image_to_ascii"
version = "0.1.0"
edition = "2021"

[dependencies]
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["ImageData"] }

[lib]
crate-type = ["cdylib", "rlib"]
